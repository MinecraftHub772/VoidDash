#include <Geode/Geode.hpp>

using namespace geode::prelude;

$on_mod(Loaded) {
    log::info("VoidDash loaded!");
    log::info("Target: Geometry Dash 2.2081");
    log::info("Code assistance: ChatGPT");
}
